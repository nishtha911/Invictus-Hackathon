{
  "name": "cognis-bank-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "One-command launcher for the Cognis Bank MVP (Next.js frontend + FastAPI backend).",
  "scripts": {
    "dev": "concurrently -k -n web,api -c cyan,green \"npm --prefix frontend run dev\" \"npm run api\"",
    "api": "cd backend && python run.py",
    "build": "npm --prefix frontend run build",
    "install:all": "npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
